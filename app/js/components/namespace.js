define(["./edictsComp", "./checksComp","./leadersComp", "./buildingsComp"], function(edicts, checks, leaders, buildings){
  return {
    edicts: edicts,
    checks: checks,
    leaders: leaders,
    buildings: buildings
  }
});
